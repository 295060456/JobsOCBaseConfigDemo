

# 关于`.gitkeep` 文件

* `.gitkeep` 文件通常用于Git仓库中的空文件夹。Git不会跟踪空文件夹，因为Git只跟踪文件的变化而不是文件夹的变化。
* 但是，有时您可能希望在Git仓库中保留某些空文件夹结构，以便在将来存放特定类型的文件或保持项目结构的一致性。
* `.gitkeep` 文件的命名是约定俗成的，它的内容通常为空，只是作为一个占位符存在。
* 当您在一个文件夹中添加了 `.gitkeep`文件时，Git就会跟踪这个文件，从而使该文件夹被纳入版本控制，即使它是空的。
* 虽然文件名中包含了 .git，但它与Git本身无关，只是作为一个约定来告诉Git保留该文件夹。